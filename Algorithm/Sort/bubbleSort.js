// Bubble Sort [Basic]

let bubbleSort = function (arr) {
    let len = arr.length
    for (let i = 0; i < len; i++) {
        for (let j = 0; j < len - i; j++) {
            console.log(j)
        }
        console.log('---')
    }

    return [];
}